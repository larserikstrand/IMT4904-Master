package no.hig.strand.lars.todoity.services;

import java.util.ArrayList;

import no.hig.strand.lars.todoity.Task;
import no.hig.strand.lars.todoity.TasksDb;
import android.app.Service;
import android.content.Intent;
import android.os.IBinder;
import android.widget.Toast;

public class RecommenderService extends Service {

	private TasksDb tasksDb;
	private ArrayList<Task> geofencedTasks;
	
	// The number of days forward in time in which to apply
	//  geofencing for the tasks.
	private static final int NUMBER_OF_GEOFENCE_DAYS = 3;
	
	@Override
	public void onCreate() {
		super.onCreate();
		
		tasksDb = TasksDb.getInstance(this);
	}

	@Override
	public void onDestroy() {
		// TODO Auto-generated method stub
		super.onDestroy();
	}

	@Override
	public int onStartCommand(Intent intent, int flags, int startId) {
		
		Toast.makeText(this, "This is recommender service", Toast.LENGTH_LONG).show();
		
		return START_STICKY;
	}

	@Override
	public IBinder onBind(Intent arg0) {
		return null;
	}

}
